# See ../.npmrc — the UI installs run from this dir (cd ui && bun install), so it
# needs its own registry pin to the public npm registry (bun reads .npmrc from
# the cwd, not parent dirs).
registry=https://registry.npmjs.org/
